openapi: 3.0.0
info:
  title: Test API
  version: 3.0.2
paths:
  /users:
    get:
      summary: Get users
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: integer
                  name:
                    type: string
  /products:
    get:
      summary: Get products
      responses:
        '200':
          description: Success
